# ================================
# CONFIGURACIÓN DOCKER - O'Higgins Stats
# ================================
# INSTRUCCIONES:
# 1. Copiar este archivo a .env.docker
# 2. Modificar los valores según tu entorno
# 3. Ejecutar: docker-compose --env-file .env.docker up -d

# ================================
# ENTORNO
# ================================
NODE_ENV=production
TZ=America/Santiago

# ================================
# BASE DE DATOS
# ================================
DB_ROOT_PASSWORD=change_this_root_password_NOW
DB_NAME=MP_DATA_DEV
DB_USER=mpuga
DB_PASSWORD=change_this_password_NOW
DB_PORT=3306

# ================================
# BACKEND
# ================================
BACKEND_PORT=3000
JWT_SECRET=change_this_secret_min_32_characters_PRODUCTION_READY

# ================================
# FRONTEND
# ================================
FRONTEND_PORT=80
# URL del backend (debe ser accesible desde el navegador del cliente)
# Para producción: usar el dominio público del backend
# Para desarrollo local: http://localhost:3000
REACT_APP_API_URL=http://localhost:3000

# ================================
# NOTAS IMPORTANTES
# ================================
# - JWT_SECRET debe tener mínimo 32 caracteres
# - DB_PASSWORD debe ser fuerte en producción
# - DB_ROOT_PASSWORD debe ser diferente a DB_PASSWORD
# - REACT_APP_API_URL debe apuntar a la URL pública del backend
# - En producción, cambiar localhost por el dominio real
